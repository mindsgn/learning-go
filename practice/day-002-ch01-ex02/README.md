
    # Day 002 - Chapter 1 Exercise 2

    **Chapter:** Setting Up Your Go Environment

    ## Original Prompt

    2. Add a target to the Makefile called clean that removes the hello_world binary and any other temporary files created by go build. Take a look at the Go command documentation to find a go command to help implement this.

    ## Acceptance

    Keep the provided Hello World program building with `make build`, then add a `clean` target to the `Makefile` that removes the generated `hello_world` binary. You can compare against `reference/` if you get stuck.

    The official answer is available in `reference/`.

Run `go test` inside this folder when you want feedback.
