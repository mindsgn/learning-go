
    # Day 006 - Chapter 2 Exercise 3

    **Chapter:** Predeclared Types and Declarations

    ## Original Prompt

    3. Write a program with three variables, one named b of type byte, one named smallI of type int32, and one named bigI of type uint64. Assign each variable the maximum legal value for its type; then add 1 to each variable. Print out their values.

    ## Acceptance

    Make your solution behave like the official reference implementation in `reference/`. Run `go test` to compare outputs.

    The official answer is available in `reference/`.

Run `go test` inside this folder when you want feedback.
