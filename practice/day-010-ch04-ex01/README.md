
# Day 010 - Chapter 4 Exercise 1

**Chapter:** Blocks, Shadows, and Control Structures

## Original Prompt

1. Write a for loop that puts 100 random numbers between 0 and 100 into an int slice.

## Acceptance

For deterministic terminal feedback, expose your loop through a helper function named `randomSlice(n int) []int`. The test only checks that it returns the right number of values in the right range.

Run `go test` inside this folder when you want feedback.
