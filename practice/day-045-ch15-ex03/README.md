
# Day 045 - Chapter 15 Exercise 3

**Chapter:** Tests

## Original Prompt

3. Write a fuzz test against the parser function and fix any problems that you find.

## Acceptance

The book references an external parser example, so this offline version provides a small local parser instead. Add a fuzz test named `FuzzParseKeyValue`, then fix any issues the fuzz run exposes.

Run `go test` inside this folder when you want feedback.
