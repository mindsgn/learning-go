
# Day 048 - Chapter 16 Exercise 3

**Chapter:** Reflection, Unsafe, and cgo

## Original Prompt

3. Copy the C code at ch16/tree/main/sample_code/mini_calc into your own module and use cgo to call it from a Go program.

## Acceptance

The required `mini_calc.c` file is already included so the exercise works offline. Implement the cgo bridge in Go and make the command-line calculator behave like the C function.

Run `go test` inside this folder when you want feedback.
